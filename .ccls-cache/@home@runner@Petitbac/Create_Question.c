/*
#include <stdio.h>
#include <stdlib.h>
#include <time.h>



char Set_Q_list(){
  char 
  srand( time( NULL ) );
  FILE * f = fopen("./Petitbac/Base donnée Question.csv", "w");
  if (f != NULL) {
      for (int i = 0; i < 5;  i++) {
          Q_list[i][0] = Q_rand_list[srand() % 10];
                  tab[ligne ][0] , tab[ligne ][1] , tab[ligne ][2]);
      }
  }
  fclose(f);

}
*/